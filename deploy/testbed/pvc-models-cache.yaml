apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: models-cache
  namespace: kserve-bench
spec:
  storageClassName: standard-host
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 80Gi
