apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-data
  namespace: minio
spec:
  storageClassName: minio-host
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 100Gi
